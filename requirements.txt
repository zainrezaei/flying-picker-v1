numpy
opencv-python
pyyaml>=6.0
rich>=13.0

# Universal Robots Official RTDE library
git+https://github.com/UniversalRobots/RTDE_Python_Client_Library.git

# -------------------------------------------------------------------
# RASPBERRY PI HARDWARE DEPENDENCIES
# -------------------------------------------------------------------
# If you are doing a fresh install on the Pi, uncomment these, OR 
# install them via system apt packages and use a virtual environment
# with the `--system-site-packages` flag.

# picamera2              # For the Pi 5 camera module
# pyserial               # Serial comms (if using any USB serial devices)
